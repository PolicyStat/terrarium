language: python
python:
    - "2.5"
    - "2.6"
    - "2.7"
env:
    - VIRTUALENV_VERSION=1.7.1.2
    - VIRTUALENV_VERSION=1.7.2
script: nosetests -v
install:
    - pip install -q virtualenv==$VIRTUALENV_VERSION --use-mirrors
    - python setup.py -q install
notifications:
  email:
    - kyle.gibson@frozenonline.com
